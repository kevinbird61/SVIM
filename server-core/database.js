// Provide app / web service & information requirement
const config = require('./config');

// definition here
class DBmodules {
    constructor(){
        
    }
}

module.exports = {
    DBmodules: new DBmodules()
};
